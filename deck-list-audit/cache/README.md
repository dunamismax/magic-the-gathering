# Local source cache

Refreshed Oracle bulk files and transient downloads live here and are excluded
from Git. The audit prefers `cache/scryfall/oracle-cards.jsonl.gz` when present
and otherwise uses the locked bootstrap corpus in `data/`.
